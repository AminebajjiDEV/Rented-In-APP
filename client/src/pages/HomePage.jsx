import NavBar from "../components/NavBar" // we didn't use {NavBar} bcs `NavBar`is a default export

const Home = () => {
    return(
       <>
       <NavBar />
       </>
    )
}

export default Home

