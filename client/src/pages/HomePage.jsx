import Header from "../components/Header"// we didn't use {Header} bcs `Header`is a default export

const Home = () => {
    return(
       <>
       <Header />
       </>
    )
}

export default Home

